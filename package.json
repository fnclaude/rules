{
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Workspace root for fnrules",
  "scripts": {
    "build": "pnpm --filter fnrules build",
    "test": "pnpm --filter fnrules-tests test",
    "typecheck": "pnpm -r typecheck",
    "package": "pnpm --filter fnrules package"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=20"
  }
}
